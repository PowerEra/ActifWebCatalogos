@model IEnumerable<ActifWebCRUD.Models.Moneda>

@{
    ViewData["Title"] = "Monedas";
}

<link rel="stylesheet" href="~/css/actif-crud.css" />
<link rel="stylesheet" type="text/css" href="https://cdn.datatables.net/1.13.4/css/dataTables.bootstrap5.min.css">

<div class="container-fluid mt-4">
    <div class="d-flex justify-content-between align-items-center mb-3">
        <h2>@ViewData["Title"]</h2>
        <div>
            <a asp-action="ExportToExcel" class="btn btn-success me-2" id="moneda_exportar_excel">
                <i class="bi bi-file-earmark-excel"></i> Exportar a Excel
            </a>
            <a asp-action="Create" class="btn btn-primary" id="moneda_agregar">
                <i class="bi bi-file-earmark-plus"></i> Agregar
            </a>
        </div>
    </div>

    <div class="table-responsive">
        <table class="table table-striped table-hover" id="moneda_datatable">
            <thead>
                <tr>
                    <th>ID</th>
                    <th>País</th>
                    <th>Nombre</th>
                    <th>Símbolo</th>
                    <th>Acciones</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.IdMoneda</td>
                        <td>@(item.Pais?.Nombre ?? "")</td>
                        <td>@item.Nombre</td>
                        <td>@item.Simbolo</td>
                        <td>
                            <a asp-action="Edit" asp-route-id="@item.IdMoneda" class="btn btn-sm btn-warning" id="moneda_editar_@item.IdMoneda" title="Editar">
                                <i class="bi bi-pencil"></i>
                            </a>
                            <a asp-action="Details" asp-route-id="@item.IdMoneda" class="btn btn-sm btn-info" id="moneda_detalles_@item.IdMoneda" title="Detalles">
                                <i class="bi bi-eye"></i>
                            </a>
                            <a asp-action="Delete" asp-route-id="@item.IdMoneda" class="btn btn-sm btn-danger" id="moneda_eliminar_@item.IdMoneda" title="Eliminar">
                                <i class="bi bi-trash"></i>
                            </a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>

@section Scripts {
    <script src="https://cdn.datatables.net/1.13.4/js/jquery.dataTables.min.js"></script>
    <script src="https://cdn.datatables.net/1.13.4/js/dataTables.bootstrap5.min.js"></script>
    <script>
        $(document).ready(function () {
            $('#moneda_datatable').DataTable({
                "language": {
                    "url": "//cdn.datatables.net/plug-ins/1.13.4/i18n/es-ES.json"
                },
                "pageLength": 50,
                "order": [[0, "asc"]],
                "columnDefs": [
                    { "orderable": false, "targets": 4 }
                ],
                initComplete: function () {
                    this.api().columns([0, 1, 2, 3]).every(function () {
                        var column = this;
                        var title = $(column.header()).text();
                        var input = $('<input type="text" class="form-control form-control-sm" placeholder="Filtrar ' + title + '" />')
                            .appendTo($(column.header()))
                            .on('click', function (e) {
                                e.stopPropagation();
                            })
                            .on('keyup change clear', function () {
                                if (column.search() !== this.value) {
                                    column.search(this.value).draw();
                                }
                            });
                    });
                }
            });
        });
    </script>
}
