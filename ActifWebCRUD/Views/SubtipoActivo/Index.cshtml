@model IEnumerable<ActifWebCRUD.Models.SubtipoActivo>

@{
    ViewData["Title"] = "Subtipos de Activo";
}

<div class="crud-container">
    <h1 class="crud-title">Subtipos de Activo</h1>

    <div class="mb-3">
        <a asp-action="Create" class="btn-add-new" id="subtipoactivo_add_button">
            <i class="fas fa-plus"></i> Agregar Nuevo Subtipo de Activo
        </a>
        <button onclick="exportToExcel()" class="btn-export-excel" id="subtipoactivo_export_button">
            <i class="fas fa-file-excel"></i> Exportar a Excel
        </button>
    </div>

    <table id="subtipoActivoTable" class="table table-striped table-bordered">
        <thead>
            <tr>
                <th>ID</th>
                <th>Tipo Activo</th>
                <th>Descripcion</th>
                <th>Actualizar</th>
                <th>Codigo</th>
                <th>Acciones</th>
            </tr>
            <tr>
                <th><input type="text" class="form-control form-control-sm" placeholder="Filtrar ID" /></th>
                <th><input type="text" class="form-control form-control-sm" placeholder="Filtrar Tipo Activo" /></th>
                <th><input type="text" class="form-control form-control-sm" placeholder="Filtrar Descripcion" /></th>
                <th><input type="text" class="form-control form-control-sm" placeholder="Filtrar Actualizar" /></th>
                <th><input type="text" class="form-control form-control-sm" placeholder="Filtrar Codigo" /></th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.IdSubtipoActivo</td>
                    <td>@item.TipoActivo?.Descripcion</td>
                    <td>@item.Descripcion</td>
                    <td>@item.Actualizar</td>
                    <td>@item.Codigo</td>
                    <td>
                        <div class="action-icons">
                            <a asp-action="Edit" asp-route-id="@item.IdSubtipoActivo" class="action-icon icon-edit" title="Editar" id="subtipoactivo_edit_@item.IdSubtipoActivo">
                                <i class="fas fa-pencil-alt"></i>
                            </a>
                            <a asp-action="Delete" asp-route-id="@item.IdSubtipoActivo" class="action-icon icon-delete" title="Eliminar" id="subtipoactivo_delete_@item.IdSubtipoActivo">
                                <i class="fas fa-trash-alt"></i>
                            </a>
                        </div>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>

@section Scripts {
    <script>
        $(document).ready(function () {
            // Initialize DataTable
            var table = $('#subtipoActivoTable').DataTable({
                "pageLength": 50,
                "language": {
                    "url": "//cdn.datatables.net/plug-ins/1.13.6/i18n/es-ES.json"
                },
                "order": [[0, "asc"]],
                "columnDefs": [
                    { "orderable": false, "targets": 5 }
                ],
                "initComplete": function () {
                    // Apply the search
                    this.api().columns().every(function (index) {
                        if (index === 5) return; // Skip actions column

                        var column = this;
                        $('input', this.header()).on('keyup change clear', function () {
                            if (column.search() !== this.value) {
                                column.search(this.value).draw();
                            }
                        });
                    });
                }
            });
        });

        function exportToExcel() {
            window.location.href = '@Url.Action("ExportToExcel", "SubtipoActivo")';
        }
    </script>
}
