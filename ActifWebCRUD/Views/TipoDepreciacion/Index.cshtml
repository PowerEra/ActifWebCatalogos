@model IEnumerable<ActifWebCRUD.Models.TipoDepreciacion>

@{
    ViewData["Title"] = "Tipos de Depreciacion";
}

<div class="crud-container">
    <h1 class="crud-title">Tipos de Depreciacion</h1>

    <div class="mb-3">
        <a asp-action="Create" class="btn-add-new" id="tipodepreciacion_add_button">
            <i class="fas fa-plus"></i> Agregar Nuevo Tipo de Depreciacion
        </a>
        <button onclick="exportToExcel()" class="btn-export-excel" id="tipodepreciacion_export_button">
            <i class="fas fa-file-excel"></i> Exportar a Excel
        </button>
    </div>

    <table id="tipoDepreciacionTable" class="table table-striped table-bordered">
        <thead>
            <tr>
                <th>ID</th>
                <th>Descripcion</th>
                <th>Mes Inicial</th>
                <th>Aplica Fiscal</th>
                <th>Acciones</th>
            </tr>
            <tr>
                <th><input type="text" class="form-control form-control-sm" placeholder="Filtrar ID" /></th>
                <th><input type="text" class="form-control form-control-sm" placeholder="Filtrar Descripcion" /></th>
                <th><input type="text" class="form-control form-control-sm" placeholder="Filtrar Mes" /></th>
                <th><input type="text" class="form-control form-control-sm" placeholder="Filtrar Fiscal" /></th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.IdTipoDep</td>
                    <td>@item.Descripcion</td>
                    <td>@item.MesIni</td>
                    <td>@item.AplicaFiscal</td>
                    <td>
                        <div class="action-icons">
                            <a asp-action="Edit" asp-route-id="@item.IdTipoDep" class="action-icon icon-edit" title="Editar" id="tipodepreciacion_edit_@item.IdTipoDep">
                                <i class="fas fa-pencil-alt"></i>
                            </a>
                            <a asp-action="Delete" asp-route-id="@item.IdTipoDep" class="action-icon icon-delete" title="Eliminar" id="tipodepreciacion_delete_@item.IdTipoDep">
                                <i class="fas fa-trash-alt"></i>
                            </a>
                        </div>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>

@section Scripts {
    <script>
        $(document).ready(function () {
            // Initialize DataTable
            var table = $('#tipoDepreciacionTable').DataTable({
                "pageLength": 50,
                "language": {
                    "url": "//cdn.datatables.net/plug-ins/1.13.6/i18n/es-ES.json"
                },
                "order": [[0, "asc"]],
                "columnDefs": [
                    { "orderable": false, "targets": 4 }
                ],
                "initComplete": function () {
                    // Apply the search
                    this.api().columns().every(function (index) {
                        if (index === 4) return; // Skip actions column

                        var column = this;
                        $('input', this.header()).on('keyup change clear', function () {
                            if (column.search() !== this.value) {
                                column.search(this.value).draw();
                            }
                        });
                    });
                }
            });
        });

        function exportToExcel() {
            window.location.href = '@Url.Action("ExportToExcel", "TipoDepreciacion")';
        }
    </script>
}
