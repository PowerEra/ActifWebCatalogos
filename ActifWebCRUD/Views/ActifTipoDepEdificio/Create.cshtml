@model ActifWebCRUD.Models.ActifTipoDepEdificio

@{
    ViewData["Title"] = "Crear Tipo Depreciación por Edificio";
}

<div class="crud-container">
    <h1 class="crud-title">Crear Nuevo Tipo Depreciación por Edificio</h1>

    <div class="form-container">
        <form asp-action="Create" id="actiftipodepedificio_create_form">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>

            <div class="row">
                <div class="col-md-6">
                    <div class="form-group">
                        <label asp-for="IdTipoDep" class="control-label"></label>
                        <select asp-for="IdTipoDep" class="form-control" asp-items="ViewBag.IdTipoDep" id="actiftipodepedificio_create_tipodep">
                            <option value="">-- Seleccione Tipo Depreciación --</option>
                        </select>
                        <span asp-validation-for="IdTipoDep" class="text-danger"></span>
                    </div>
                </div>
                <div class="col-md-6">
                    <div class="form-group">
                        <label asp-for="IdEdificio" class="control-label"></label>
                        <select asp-for="IdEdificio" class="form-control" asp-items="ViewBag.IdEdificio" id="actiftipodepedificio_create_edificio">
                            <option value="">-- Seleccione Edificio --</option>
                        </select>
                        <span asp-validation-for="IdEdificio" class="text-danger"></span>
                    </div>
                </div>
            </div>

            <div class="row">
                <div class="col-md-6">
                    <div class="form-group">
                        <label asp-for="FechaCaptura" class="control-label"></label>
                        <input asp-for="FechaCaptura" class="form-control" type="datetime-local" id="actiftipodepedificio_create_fecha" />
                        <span asp-validation-for="FechaCaptura" class="text-danger"></span>
                    </div>
                </div>
            </div>

            <div class="form-group mt-3">
                <button type="submit" class="btn btn-primary" id="actiftipodepedificio_create_submit">Guardar</button>
                <a asp-action="Index" class="btn btn-secondary" id="actiftipodepedificio_create_cancel">Cancelar</a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
