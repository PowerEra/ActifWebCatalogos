@model ActifWebCRUD.Models.EdificioPiso

@{
    ViewData["Title"] = "Crear Edificio Piso";
}

<div class="crud-container">
    <h1 class="crud-title">Crear Nueva Relación Edificio-Piso</h1>

    <div class="form-container">
        <form asp-action="Create" id="edificiopiso_create_form">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>

            <div class="row">
                <div class="col-md-6">
                    <div class="form-group">
                        <label asp-for="IdCompania" class="control-label"></label>
                        <select asp-for="IdCompania" class="form-control" asp-items="ViewBag.IdCompania" id="edificiopiso_create_compania">
                            <option value="">-- Seleccione Compañía --</option>
                        </select>
                        <span asp-validation-for="IdCompania" class="text-danger"></span>
                    </div>
                </div>
                <div class="col-md-6">
                    <div class="form-group">
                        <label asp-for="IdEdificio" class="control-label"></label>
                        <select name="IdEdificio" id="edificiopiso_create_edificio" class="form-control">
                            <option value="">-- Seleccione Edificio --</option>
                            @foreach (var item in (List<SelectListItem>)ViewData["IdEdificio"])
                            {
                                <option value="@item.Value">@item.Text</option>
                            }
                        </select>
                        <span asp-validation-for="IdEdificio" class="text-danger"></span>
                    </div>
                </div>
            </div>

            <div class="row">
                <div class="col-md-6">
                    <div class="form-group">
                        <label asp-for="IdPiso" class="control-label"></label>
                        <select asp-for="IdPiso" class="form-control" asp-items="ViewBag.IdPiso" id="edificiopiso_create_piso">
                            <option value="">-- Seleccione Piso --</option>
                        </select>
                        <span asp-validation-for="IdPiso" class="text-danger"></span>
                    </div>
                </div>
            </div>

            <div class="form-group mt-3">
                <button type="submit" class="btn btn-primary" id="edificiopiso_create_submit">Guardar</button>
                <a asp-action="Index" class="btn btn-secondary" id="edificiopiso_create_cancel">Cancelar</a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
