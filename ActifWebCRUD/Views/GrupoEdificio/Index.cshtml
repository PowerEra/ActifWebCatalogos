@model IEnumerable<ActifWebCRUD.Models.GrupoEdificio>

@{
    ViewData["Title"] = "Grupo Edificio";
}

<div class="crud-container">
    <h1 class="crud-title">Grupo Edificio</h1>

    <div class="mb-3">
        <a asp-action="Create" class="btn-add-new" id="grupoedificio_add_button">
            <i class="fas fa-plus"></i> Agregar Nuevo Grupo
        </a>
        <button onclick="exportToExcel()" class="btn-export-excel" id="grupoedificio_export_button">
            <i class="fas fa-file-excel"></i> Exportar a Excel
        </button>
    </div>

    <table id="grupoEdificioTable" class="table table-striped table-bordered">
        <thead>
            <tr>
                <th>ID Grupo</th>
                <th>Grupo</th>
                <th>Acciones</th>
            </tr>
            <tr>
                <th><input type="text" class="form-control form-control-sm" placeholder="Filtrar ID" /></th>
                <th><input type="text" class="form-control form-control-sm" placeholder="Filtrar Grupo" /></th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.IdGrupo</td>
                    <td>@item.Grupo</td>
                    <td>
                        <div class="action-icons">
                            <a asp-action="Edit" asp-route-id="@item.IdGrupo" class="action-icon icon-edit" title="Editar" id="grupoedificio_edit_@item.IdGrupo">
                                <i class="fas fa-pencil-alt"></i>
                            </a>
                            <a asp-action="Delete" asp-route-id="@item.IdGrupo" class="action-icon icon-delete" title="Eliminar" id="grupoedificio_delete_@item.IdGrupo">
                                <i class="fas fa-trash-alt"></i>
                            </a>
                        </div>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>

@section Scripts {
    <script>
        $(document).ready(function () {
            // Initialize DataTable
            var table = $('#grupoEdificioTable').DataTable({
                "pageLength": 50,
                "language": {
                    "url": "//cdn.datatables.net/plug-ins/1.13.6/i18n/es-ES.json"
                },
                "order": [[0, "asc"]],
                "columnDefs": [
                    { "orderable": false, "targets": 2 }
                ],
                "initComplete": function () {
                    // Apply the search
                    this.api().columns().every(function (index) {
                        if (index === 2) return; // Skip actions column

                        var column = this;
                        $('input', this.header()).on('keyup change clear', function () {
                            if (column.search() !== this.value) {
                                column.search(this.value).draw();
                            }
                        });
                    });
                }
            });
        });

        function exportToExcel() {
            window.location.href = '@Url.Action("ExportToExcel", "GrupoEdificio")';
        }
    </script>
}
