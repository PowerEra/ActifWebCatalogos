@model ActifWebCRUD.Models.EdificioCentroCosto

@{
    ViewData["Title"] = "Editar Edificio Centro Costo";
}

<div class="crud-container">
    <h1 class="crud-title">Editar Relación Edificio-Centro Costo</h1>

    <div class="form-container">
        <form asp-action="Edit" id="edificiocentrocosto_edit_form">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="IdEdificioCentroCosto" />

            <div class="row">
                <div class="col-md-6">
                    <div class="form-group">
                        <label asp-for="IdCompania" class="control-label"></label>
                        <select asp-for="IdCompania" class="form-control" asp-items="ViewBag.IdCompania" id="edificiocentrocosto_edit_compania">
                            <option value="">-- Seleccione Compañía --</option>
                        </select>
                        <span asp-validation-for="IdCompania" class="text-danger"></span>
                    </div>
                </div>
                <div class="col-md-6">
                    <div class="form-group">
                        <label asp-for="IdEdificio" class="control-label"></label>
                        <select name="IdEdificio" id="edificiocentrocosto_edit_edificio" class="form-control">
                            <option value="">-- Seleccione Edificio --</option>
                            @foreach (var item in (List<SelectListItem>)ViewData["IdEdificio"])
                            {
                                <option value="@item.Value" selected="@item.Selected">@item.Text</option>
                            }
                        </select>
                        <span asp-validation-for="IdEdificio" class="text-danger"></span>
                    </div>
                </div>
            </div>

            <div class="row">
                <div class="col-md-6">
                    <div class="form-group">
                        <label asp-for="IdCentroCosto" class="control-label"></label>
                        <select name="IdCentroCosto" id="edificiocentrocosto_edit_centrocosto" class="form-control">
                            <option value="">-- Seleccione Centro de Costo --</option>
                            @foreach (var item in (List<SelectListItem>)ViewData["IdCentroCosto"])
                            {
                                <option value="@item.Value" selected="@item.Selected">@item.Text</option>
                            }
                        </select>
                        <span asp-validation-for="IdCentroCosto" class="text-danger"></span>
                    </div>
                </div>
            </div>

            <div class="form-group mt-3">
                <button type="submit" class="btn btn-primary" id="edificiocentrocosto_edit_submit">Guardar Cambios</button>
                <a asp-action="Index" class="btn btn-secondary" id="edificiocentrocosto_edit_cancel">Cancelar</a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
