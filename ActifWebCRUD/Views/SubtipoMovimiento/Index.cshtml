@model IEnumerable<ActifWebCRUD.Models.SubtipoMovimiento>

@{
    ViewData["Title"] = "Subtipos de Movimiento";
}

<div class="crud-container">
    <h1 class="crud-title">Subtipos de Movimiento</h1>

    <div class="mb-3">
        <a asp-action="Create" class="btn-add-new" id="subtipomovimiento_add_button">
            <i class="fas fa-plus"></i> Agregar Nuevo Subtipo de Movimiento
        </a>
        <button onclick="exportToExcel()" class="btn-export-excel" id="subtipomovimiento_export_button">
            <i class="fas fa-file-excel"></i> Exportar a Excel
        </button>
    </div>

    <table id="subtipoMovimientoTable" class="table table-striped table-bordered">
        <thead>
            <tr>
                <th>ID</th>
                <th>Tipo Movimiento</th>
                <th>Descripcion</th>
                <th>Afectacion Interfaz</th>
                <th>Flag Visualizar</th>
                <th>Flag Graba Historial</th>
                <th>Acciones</th>
            </tr>
            <tr>
                <th><input type="text" class="form-control form-control-sm" placeholder="Filtrar ID" /></th>
                <th><input type="text" class="form-control form-control-sm" placeholder="Filtrar Tipo Movimiento" /></th>
                <th><input type="text" class="form-control form-control-sm" placeholder="Filtrar Descripcion" /></th>
                <th><input type="text" class="form-control form-control-sm" placeholder="Filtrar Afectacion" /></th>
                <th><input type="text" class="form-control form-control-sm" placeholder="Filtrar Visualizar" /></th>
                <th><input type="text" class="form-control form-control-sm" placeholder="Filtrar Graba Hist" /></th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.IdSubtipoMov</td>
                    <td>@item.TipoMovimiento?.Descripcion</td>
                    <td>@item.Descripcion</td>
                    <td>@item.AfectacionInterfaz</td>
                    <td>@item.FlgVisualizar</td>
                    <td>@item.FlgGrabahist</td>
                    <td>
                        <div class="action-icons">
                            <a asp-action="Edit" asp-route-id="@item.IdSubtipoMov" class="action-icon icon-edit" title="Editar" id="subtipomovimiento_edit_@item.IdSubtipoMov">
                                <i class="fas fa-pencil-alt"></i>
                            </a>
                            <a asp-action="Delete" asp-route-id="@item.IdSubtipoMov" class="action-icon icon-delete" title="Eliminar" id="subtipomovimiento_delete_@item.IdSubtipoMov">
                                <i class="fas fa-trash-alt"></i>
                            </a>
                        </div>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>

@section Scripts {
    <script>
        $(document).ready(function () {
            // Initialize DataTable
            var table = $('#subtipoMovimientoTable').DataTable({
                "pageLength": 50,
                "language": {
                    "url": "//cdn.datatables.net/plug-ins/1.13.6/i18n/es-ES.json"
                },
                "order": [[0, "asc"]],
                "columnDefs": [
                    { "orderable": false, "targets": 6 }
                ],
                "initComplete": function () {
                    // Apply the search
                    this.api().columns().every(function (index) {
                        if (index === 6) return; // Skip actions column

                        var column = this;
                        $('input', this.header()).on('keyup change clear', function () {
                            if (column.search() !== this.value) {
                                column.search(this.value).draw();
                            }
                        });
                    });
                }
            });
        });

        function exportToExcel() {
            window.location.href = '@Url.Action("ExportToExcel", "SubtipoMovimiento")';
        }
    </script>
}
